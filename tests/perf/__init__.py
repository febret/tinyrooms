"""Performance regression suite.

These tests are excluded from ``python -m unittest discover -s tests`` because
none of the module names match the default ``test*.py`` pattern. Run them with
``npm run test:perf``.
"""
